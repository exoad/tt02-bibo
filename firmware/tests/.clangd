CompileFlags:
  # This directory, not firmware/build: the parent firmware/.clangd points at the
  # CMake database, which holds only ARM cross-compiled files, so a host test
  # here would inherit --target=arm-none-eabi.
  CompilationDatabase: .
