[Unit]
Description=bibo: switch to the field hotspot when it is in the air
After=NetworkManager.service

[Service]
Type=oneshot
# install.sh rewrites this path to the checkout's location.
ExecStart=/bin/sh /home/jack/tt02-bibo/firmware/pilot/tools/status/prefer-hotspot.sh
# No [Install] section: the timer is what gets enabled, and it starts this.
